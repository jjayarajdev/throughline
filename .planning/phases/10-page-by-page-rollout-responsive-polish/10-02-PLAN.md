---
phase: 10-page-by-page-rollout-responsive-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/company/CompanyWallet.tsx
  - apps/web/src/pages/company/CompanyFund.tsx
  - apps/web/src/pages/admin/AdminPayouts.tsx
  - apps/web/src/pages/admin/AdminSettings.tsx
autonomous: true
requirements: [FR-19, FR-23, NFR-03]
must_haves:
  truths:
    - "CompanyWallet shows balance cards using StatCardV2 with design tokens, and recent transactions in a Card with semantic colors"
    - "CompanyFund uses Card, Button, Input, and Label from design system -- zero hardcoded emerald/gray/red colors"
    - "AdminPayouts uses DataTable for payout requests with StatusBadge, Button for actions, and Dialog for reject modal"
    - "AdminSettings uses Card, Input, and Button from design system with Skeleton loading"
    - "Zero hardcoded color values remain in any of the 4 files"
  artifacts:
    - path: "apps/web/src/pages/company/CompanyWallet.tsx"
      provides: "Company wallet page with StatCardV2 balance + design system transactions"
      contains: "StatCardV2"
    - path: "apps/web/src/pages/company/CompanyFund.tsx"
      provides: "Fund wallet page with Card + Button + Input design system components"
      contains: "Card"
    - path: "apps/web/src/pages/admin/AdminPayouts.tsx"
      provides: "Admin payouts page with DataTable + StatusBadge + Dialog"
      contains: "DataTable"
    - path: "apps/web/src/pages/admin/AdminSettings.tsx"
      provides: "Admin settings page with Card + Input + Button"
      contains: "Card"
  key_links:
    - from: "apps/web/src/pages/company/CompanyWallet.tsx"
      to: "@/components/custom/StatCardV2"
      via: "import"
      pattern: "StatCardV2"
    - from: "apps/web/src/pages/admin/AdminPayouts.tsx"
      to: "@/components/data-table/DataTable"
      via: "import"
      pattern: "DataTable"
---

<objective>
Convert the 4 remaining heavily unconverted pages: CompanyWallet, CompanyFund (company financial), AdminPayouts, AdminSettings (admin pages). These all use raw HTML elements, hardcoded color classes (emerald, gray, purple, amber, red), raw buttons/inputs, and spinner/text loading states.

Purpose: Eliminate all legacy styling from company financial and admin pages.
Output: 4 fully converted pages using design system components (StatCardV2, DataTable, Card, Button, Input, StatusBadge, Skeleton, Dialog).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-page-by-page-rollout-responsive-polish/10-RESEARCH.md

<interfaces>
<!-- Same component APIs as Plan 01 plus Dialog -->

From apps/web/src/components/ui/dialog.tsx:
```typescript
<Dialog open={open} onOpenChange={setOpen}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Title</DialogTitle>
      <DialogDescription>Description</DialogDescription>
    </DialogHeader>
    {/* body */}
    <DialogFooter>
      <Button variant="outline" onClick={() => setOpen(false)}>Cancel</Button>
      <Button onClick={handleAction}>Confirm</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

From apps/web/src/components/ui/select.tsx:
```typescript
// Project uses native HTML select wrapper, NOT Radix Select
<Select value={val} onChange={handler}>
  <option value="">All</option>
  <option value="x">X</option>
</Select>
```

From apps/web/src/components/ui/label.tsx:
```typescript
<Label htmlFor="inputId">Label text</Label>
```

StatCardV2, DataTable, DataTableColumnHeader, StatusBadge, PageHeader, EmptyState, Skeleton, Button, Card, Input, Badge — same APIs as Plan 01 interfaces section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert CompanyWallet and CompanyFund to design system</name>
  <files>apps/web/src/pages/company/CompanyWallet.tsx, apps/web/src/pages/company/CompanyFund.tsx</files>
  <action>
  **CompanyWallet.tsx** -- Full rewrite:

  1. **Header**: Replace raw `h1` + raw `Link className="bg-emerald-600"` with `PageHeader title="Wallet"` and a `Button asChild` wrapping `<Link to="/c/wallet/fund" viewTransition>Add Funds</Link>` with ArrowRight icon (or Plus icon).

  2. **Balance cards**: Remove the local `Card` function component entirely (it shadows the shadcn Card import). Replace with 3 `StatCardV2` in `grid gap-4 sm:grid-cols-2 lg:grid-cols-3`:
     - "Available" = balance - lockedBalance, use Wallet icon
     - "Locked" = lockedBalance, use Lock icon
     - "Total Balance" = balance, gradient variant
     Remove ALL `bg-emerald-50`, `bg-amber-50`, `bg-blue-50`, `dark:bg-*-900/20` classes.

  3. **Loading**: Replace `animate-pulse rounded-xl bg-gray-100` divs with `<Skeleton className="h-32" />`.

  4. **Transactions section**: Replace raw `h2` + raw transaction rows with a `Card` wrapping the list. Each `TxRow` should use:
     - `text-success` for credit amounts (not `text-emerald-600`)
     - `text-destructive` for debit amounts (not `text-red-600`)
     - `bg-muted` / `text-muted-foreground` for expanded details (not `bg-gray-50`, `text-gray-500`)
     - `hover:bg-accent` on row buttons (not `hover:bg-gray-50`)
     - `border-border` (not `dark:border-gray-700`)
     - `ChevronDown`/`ChevronUp` icons use `text-muted-foreground` (not `text-gray-400`)
     Keep the expandable TxRow pattern but reclass everything with semantic tokens.

  5. **Empty transactions**: Replace raw `<Wallet>` + `<p>No transactions yet</p>` with `<EmptyState title="No transactions yet" description="Fund your wallet to get started." action={{ label: "Add Funds", href: "/c/wallet/fund" }} icon={Wallet} />`.

  6. **View all link**: Replace `text-emerald-600 hover:underline` with `text-primary hover:underline` or use a `Button variant="link"`.

  **CompanyFund.tsx** -- Full rewrite:

  1. **Back button**: Replace raw `<button className="text-gray-500">` with `<Button variant="ghost" size="sm" onClick={...}><ArrowLeft /> Back to wallet</Button>`.

  2. **Title**: Replace raw `<h1>` with `<PageHeader title="Add funds" description="Choose an amount to fund your company wallet" />`.

  3. **Preset buttons**: Replace raw `<button className="rounded-lg border ..." >` with `<Button variant={amount === p ? 'default' : 'outline'}>`. The selected state uses primary color (not `border-emerald-600 bg-emerald-50`). Wrap in `grid grid-cols-2 gap-3 sm:grid-cols-4`.

  4. **Custom input**: Replace raw `<label className="text-gray-600">` with `<Label>`. Replace raw `<input>` with `<Input>`. Wrap rupee symbol in a proper prefix pattern using relative positioning with `text-muted-foreground` (not `text-gray-400`).

  5. **Error**: Replace `text-red-600` with `text-destructive`.

  6. **Pay button**: Replace raw `<button className="bg-emerald-600">` with `<Button className="w-full" size="lg">`. Disabled state handled by Button's built-in disabled styling.

  7. Wrap the entire form area in a `Card` > `CardContent` for visual containment.

  Remove ALL `bg-emerald-*`, `hover:bg-emerald-*`, `text-emerald-*`, `border-emerald-*`, `bg-gray-*`, `dark:bg-gray-*`, `dark:border-gray-*`, `text-gray-*` classes from both files.
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CompanyWallet uses StatCardV2 for balances, semantic tokens for transactions, EmptyState. CompanyFund uses Card, Button, Input, Label. Zero hardcoded colors in both files. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Convert AdminPayouts and AdminSettings to design system</name>
  <files>apps/web/src/pages/admin/AdminPayouts.tsx, apps/web/src/pages/admin/AdminSettings.tsx</files>
  <action>
  **AdminPayouts.tsx** -- Full rewrite:

  1. **Header**: Replace raw `h1` + raw `<button className="bg-purple-600">` with `PageHeader title="Payout Management"` and `<Button onClick={...} disabled={runBatch.isPending}><Play /> {text}</Button>`.

  2. **Filter**: Replace raw `<select className="border dark:border-gray-700 dark:bg-gray-800">` with `<Select>` component. Add a `Label` or place inline.

  3. **Requests table**: Replace entire raw `<table>` with `DataTable`. Define columns via `useMemo`:
     - Recruiter (text, truncated recruiter ID -- NOTE: API returns recruiterId, not name)
     - Amount (right-aligned, INR formatted, `font-medium`)
     - Status (use `StatusBadge` -- remove the entire inline `className={...}` ternary chain)
     - Date (formatted, `text-muted-foreground`)
     - Actions (conditional: if status==='pending_approval', show `<Button size="sm" onClick={approve}><CheckCircle /> Approve</Button>` and `<Button size="sm" variant="destructive" onClick={() => setRejectId(r.id)}><XCircle /> Reject</Button>`)
     Server-side pagination with page/pageSize state.

  4. **Loading**: Replace `animate-pulse bg-gray-100` with Skeleton rows.

  5. **Empty**: Replace `<p className="text-gray-400">` with `<EmptyState title="No payout requests found" icon={Banknote} />`.

  6. **Reject modal**: Replace the raw `<div className="fixed inset-0 ... bg-black/40">` DIV-based modal with proper `<Dialog>` component:
     ```
     <Dialog open={!!rejectId} onOpenChange={(open) => { if (!open) { setRejectId(null); setRejectReason(''); } }}>
       <DialogContent>
         <DialogHeader>
           <DialogTitle>Reject payout request</DialogTitle>
         </DialogHeader>
         <Textarea value={rejectReason} onChange={...} placeholder="Reason for rejection..." rows={3} />
         <DialogFooter>
           <Button variant="outline" onClick={close}>Cancel</Button>
           <Button variant="destructive" onClick={doReject} disabled={!rejectReason.trim()}>Reject</Button>
         </DialogFooter>
       </DialogContent>
     </Dialog>
     ```

  7. **Batch history table**: Replace raw `<table>` with a second `DataTable` or a simple styled `Table` (from shadcn ui/table). Columns: Batch ID (mono, truncated), Requests count, Total (INR), Status (StatusBadge), Created (date). Wrap in `Card`.

  8. Remove ALL `bg-gray-*`, `dark:bg-gray-*`, `bg-purple-*`, `hover:bg-purple-*`, `bg-emerald-*`, `hover:bg-emerald-*`, `bg-amber-*`, `bg-red-*`, `hover:bg-red-*`, `bg-blue-*`, `text-gray-*`, `dark:divide-gray-*`, `dark:border-gray-*` classes.

  **AdminSettings.tsx** -- Full rewrite:

  1. **Loading**: Replace `<Loader2 className="text-purple-500">` spinner with Skeleton cards: `<div className="space-y-4">{Array.from({length: 3}).map((_,i) => <Skeleton key={i} className="h-24 w-full" />)}</div>`.

  2. **Header**: Replace raw `<h1>` with `<PageHeader title="Platform Settings" description="Configure platform-wide settings" />`.

  3. **Settings cards**: Replace raw `<div className="rounded-xl border dark:border-gray-700">` with `<Card>` + `<CardContent>`. Each setting:
     - Label: `<Label className="text-sm font-semibold">{s.key}</Label>`
     - Description: `<p className="text-xs text-muted-foreground">`
     - Input: Replace raw `<input className="border dark:border-gray-700 dark:bg-gray-800">` with `<Input>` component
     - Save button: Replace raw `<button className="bg-purple-600">` with `<Button size="sm"><Save /> Save</Button>`
     - Success indicator: Replace `text-emerald-600` with `text-success`
     - Last updated: Use `text-muted-foreground` (not `text-gray-400`)

  4. **Empty**: Replace `<p className="text-gray-400">` with `<EmptyState title="No settings configured" icon={Settings} />`.

  5. Remove ALL `bg-purple-*`, `hover:bg-purple-*`, `text-emerald-*`, `text-gray-*`, `dark:border-gray-*`, `dark:bg-gray-*`, `text-purple-*` classes.
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>AdminPayouts uses DataTable + StatusBadge + Dialog for reject modal + Button for actions. AdminSettings uses Card + Input + Button + Skeleton + Label. Zero hardcoded colors in both files. TypeScript compiles.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `grep -r "bg-gray-\|text-gray-\|border-gray-\|bg-emerald-\|bg-purple-\|bg-amber-\|bg-blue-\|bg-red-\|hover:bg-emerald-\|hover:bg-purple-\|hover:bg-red-\|dark:bg-gray\|dark:border-gray\|dark:text-gray\|dark:divide-gray\|text-emerald-\|text-purple-" apps/web/src/pages/company/CompanyWallet.tsx apps/web/src/pages/company/CompanyFund.tsx apps/web/src/pages/admin/AdminPayouts.tsx apps/web/src/pages/admin/AdminSettings.tsx` returns 0 matches
4. AdminPayouts imports Dialog from `@/components/ui/dialog`
</verification>

<success_criteria>
- CompanyWallet: StatCardV2 balance cards, semantic transaction list, EmptyState
- CompanyFund: Card + Button + Input, preset selection with Button variant toggle
- AdminPayouts: DataTable + StatusBadge + Dialog reject modal + Skeleton loading
- AdminSettings: Card + Input + Button + Skeleton loading + Label
- Zero hardcoded colors across all 4 files
- TypeScript compilation passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-by-page-rollout-responsive-polish/10-02-SUMMARY.md`
</output>

---
phase: 10-page-by-page-rollout-responsive-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/recruiter/RecruiterWallet.tsx
  - apps/web/src/pages/recruiter/RecruiterEarnings.tsx
  - apps/web/src/pages/recruiter/RecruiterPayouts.tsx
autonomous: true
requirements: [FR-19, NFR-03]
must_haves:
  truths:
    - "RecruiterWallet shows balance cards using StatCardV2 with skeleton loading, and transactions via DataTable with pagination"
    - "RecruiterEarnings shows summary stat cards using StatCardV2 with design tokens, and earnings table via DataTable with sort/filter"
    - "RecruiterPayouts shows payout history via DataTable with StatusBadge, and withdrawal dialog uses Button/Dialog design system components"
    - "Zero hardcoded color values (bg-gray-*, text-gray-*, bg-blue-*, border-gray-*) remain in any of the 3 files"
  artifacts:
    - path: "apps/web/src/pages/recruiter/RecruiterWallet.tsx"
      provides: "Wallet page with StatCardV2 balance cards + DataTable transactions"
      contains: "StatCardV2"
    - path: "apps/web/src/pages/recruiter/RecruiterEarnings.tsx"
      provides: "Earnings page with StatCardV2 summary + DataTable earnings list"
      contains: "DataTable"
    - path: "apps/web/src/pages/recruiter/RecruiterPayouts.tsx"
      provides: "Payouts page with DataTable history + design system withdrawal button"
      contains: "DataTable"
  key_links:
    - from: "apps/web/src/pages/recruiter/RecruiterWallet.tsx"
      to: "@/components/custom/StatCardV2"
      via: "import"
      pattern: "StatCardV2"
    - from: "apps/web/src/pages/recruiter/RecruiterEarnings.tsx"
      to: "@/components/data-table/DataTable"
      via: "import"
      pattern: "DataTable"
    - from: "apps/web/src/pages/recruiter/RecruiterPayouts.tsx"
      to: "@/components/data-table/DataTable"
      via: "import"
      pattern: "DataTable"
---

<objective>
Convert the 3 recruiter financial pages (Wallet, Earnings, Payouts) from raw HTML with hardcoded colors to the Phase 5-9 design system. These are the heaviest unconverted recruiter pages -- they use raw tables, raw buttons, hardcoded gray/blue/green/red/yellow classes, and "Loading..." text instead of skeletons.

Purpose: Eliminate all legacy styling from recruiter financial pages so users see consistent design language.
Output: 3 fully converted pages using StatCardV2, DataTable, StatusBadge, PageHeader, Skeleton, and semantic tokens.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-page-by-page-rollout-responsive-polish/10-RESEARCH.md

<interfaces>
<!-- Executor needs these component APIs. All exist from Phases 6-8. -->

From apps/web/src/components/custom/StatCardV2.tsx:
```typescript
interface StatCardV2Props {
  icon?: ReactNode;
  label: string;
  value?: ReactNode;  // Accepts AnimatedMetric or string/number
  helper?: ReactNode;
  variant?: 'default' | 'gradient';
  className?: string;
}
```

From apps/web/src/components/data-table/DataTable.tsx:
```typescript
// Client-side mode: omit pageCount, DataTable handles pagination/sort/filter internally
// Server-side mode: provide pageCount + pagination + onPaginationChange
<DataTable columns={columns} data={data} pageCount={N} pagination={state} onPaginationChange={setter} />
```

From apps/web/src/components/data-table/DataTableColumnHeader.tsx:
```typescript
<DataTableColumnHeader column={column} title="Column Name" />
```

From apps/web/src/components/StatusBadge.tsx:
```typescript
<StatusBadge status={statusString} />
// Maps statuses to badge variants: pending->warning, approved/success->success, rejected->destructive, etc.
```

From apps/web/src/components/custom/PageHeader.tsx:
```typescript
<PageHeader title="Page Title" description="Subtitle text" />
```

From apps/web/src/components/shared/EmptyState.tsx:
```typescript
<EmptyState title="No items" description="Help text" action={{ label: "CTA", href: "/path" }} icon={IconComponent} />
```

Badge variant mapping (standardized in Phase 7):
- pending/pending_approval -> warning
- approved/completed/paid/payable -> success
- rejected/failed/cancelled -> destructive
- processing -> default (or secondary)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert RecruiterWallet to design system</name>
  <files>apps/web/src/pages/recruiter/RecruiterWallet.tsx</files>
  <action>
  Rewrite RecruiterWallet.tsx replacing all legacy patterns with design system components:

  1. **Header**: Replace raw `h1` + raw `a` button with `PageHeader` title="Wallet" and a `Button asChild` with `Link to="/r/payouts"` using ArrowDownToLine icon. Use `viewTransition` on the Link.

  2. **Balance section**: Replace `WalletBalanceCard` import with 3 inline `StatCardV2` cards in a responsive grid (`grid gap-4 sm:grid-cols-2 lg:grid-cols-3`):
     - "Available Balance" = `Number(balance) - Number(lockedBalance)`, format with INR, use Wallet icon
     - "Locked Balance" = `lockedBalance`, format INR, use Lock icon
     - "Total Balance" = `balance`, format INR, gradient variant

  3. **Loading state**: Replace `<p className="text-gray-400">Loading balance...</p>` with Skeleton cards (`<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Skeleton className="h-32" /><Skeleton className="h-32" /><Skeleton className="h-32" /></div>`).

  4. **Transactions section**: Replace raw `h2` + `TransactionTable` import with a `Card` containing a `DataTable`. Define columns using `useMemo`:
     - Date (sortable via DataTableColumnHeader, format with toLocaleDateString)
     - Description (from tx.description or tx.transactionType)
     - Type (Badge: "Credit" variant="success" / "Debit" variant="destructive")
     - Amount (formatted INR, text-success for credits, text-destructive for debits)
     - Balance After (formatted INR)
     Use server-side pagination: `pageCount`, `pagination` state, `onPaginationChange`.

  5. **Loading for transactions**: Replace "Loading transactions..." with `<Skeleton className="h-10 w-full" />` repeated 3 times in `space-y-2`.

  6. **Empty state**: If no transactions, show `<EmptyState title="No transactions yet" description="Fund your wallet or earn from placements to see transactions here." icon={Wallet} />`.

  7. **Remove** `WalletBalanceCard` and `TransactionTable` imports (they are legacy components). Remove ALL hardcoded color classes: `text-gray-400`, `text-gray-900`, `dark:text-white`, `bg-blue-600`, `hover:bg-blue-700`.

  INR formatter: `const fmtInr = (v: string | number) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' }).format(Number(v));`
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>RecruiterWallet uses StatCardV2 for balances, DataTable for transactions, Skeleton for loading, EmptyState for empty. Zero hardcoded colors. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Convert RecruiterEarnings and RecruiterPayouts to design system</name>
  <files>apps/web/src/pages/recruiter/RecruiterEarnings.tsx, apps/web/src/pages/recruiter/RecruiterPayouts.tsx</files>
  <action>
  **RecruiterEarnings.tsx** -- Full rewrite of layout and table:

  1. **Header**: Replace `<h1 className="text-gray-900 dark:text-white">` with `<PageHeader title="Earnings" description="Track your earnings from successful placements" />`.

  2. **Summary cards**: Replace the raw `div` cards with 4 `StatCardV2` in `grid gap-4 sm:grid-cols-2 lg:grid-cols-4`:
     - "Total Earned" with `fmtInr(s.totalEarned)`, gradient variant
     - "Pending" with `fmtInr(s.totalPending)`, icon=Clock
     - "Payable" with `fmtInr(s.totalPayable)`, icon=Banknote
     - "Paid" with `fmtInr(s.totalPaid)`, icon=CheckCircle2
     Remove ALL hardcoded color classes from summary cards (`text-yellow-600`, `text-blue-600`, `text-green-600`, `bg-white`, `dark:bg-gray-800`, `border-gray-200`, `dark:border-gray-700`).

  3. **Filters**: Replace raw `<select>` elements with `Select` from `@/components/ui/select` styled with design tokens. Wrap in a `DataTableToolbar`-style div or just `flex flex-wrap gap-3`.

  4. **Table**: Replace entire raw `<table>` with `DataTable`. Define columns via `useMemo`:
     - Date (sortable DataTableColumnHeader, formatted)
     - Role (text, truncated)
     - Type (Badge: "Shortlist" / "Hire")
     - Gross (right-aligned, INR formatted)
     - Commission (right-aligned, INR, `text-destructive`)
     - Net (right-aligned, INR, `font-medium`)
     - Status (StatusBadge component)
     Use server-side pagination with `pageCount` from `Math.ceil(data.total / pageSize)`.

  5. **Loading**: Replace "Loading..." with `Skeleton` rows. **Empty**: Replace raw text with `EmptyState` (title="No earnings yet", action to Browse Roles).

  6. Remove ALL hardcoded classes: `text-gray-900`, `dark:text-white`, `bg-gray-50`, `dark:bg-gray-800`, `border-gray-200`, `dark:border-gray-700`, `text-gray-400`, `text-gray-500`, `text-gray-700`, `dark:text-gray-300`, `dark:text-gray-400`, `text-red-500`, `bg-white`, `dark:bg-gray-800/50`.

  **RecruiterPayouts.tsx** -- Full rewrite:

  1. **Header**: Replace raw `h1` + raw `button` with `PageHeader title="Payouts"` and `Button onClick={() => setDialogOpen(true)}` with ArrowDownToLine icon text "Request Withdrawal".

  2. **Table**: Replace entire raw `<table>` with `DataTable`. Columns:
     - Date (sortable, formatted)
     - Amount (right-aligned, INR, `font-medium`)
     - Status (use `StatusBadge` component -- remove entire `STATUS_STYLES` record and `statusLabel` function)
     - Admin Note (text, fallback "—")
     Server-side pagination.

  3. **Loading/Empty**: Skeleton / EmptyState ("No payout requests yet", action text "Request your first withdrawal").

  4. **Withdraw button**: Replace raw `<button className="bg-blue-600">` with `<Button>` from ui/button.

  5. Remove ALL `STATUS_STYLES` map (replaced by StatusBadge), all `bg-gray-*`, `border-gray-*`, `text-gray-*`, `bg-blue-*`, `hover:bg-blue-*`, `dark:*` hardcoded patterns.

  Both files: ensure `viewTransition` on any `Link`, add responsive grid classes, use `Card` wrapper around DataTable content.
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>RecruiterEarnings uses StatCardV2 for summary, DataTable for list, StatusBadge for statuses. RecruiterPayouts uses DataTable with StatusBadge, Button for withdraw action. Zero hardcoded colors in both files. TypeScript compiles.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `grep -r "bg-gray-\|text-gray-\|border-gray-\|bg-blue-\|bg-yellow-\|bg-green-\|bg-red-\|dark:bg-gray\|dark:text-gray\|dark:border-gray" apps/web/src/pages/recruiter/RecruiterWallet.tsx apps/web/src/pages/recruiter/RecruiterEarnings.tsx apps/web/src/pages/recruiter/RecruiterPayouts.tsx` returns 0 matches
4. All 3 pages import from `@/components/data-table` or `@/components/custom`
</verification>

<success_criteria>
- RecruiterWallet: StatCardV2 balance cards + DataTable transactions + Skeleton loading + EmptyState
- RecruiterEarnings: StatCardV2 summary + DataTable with sort/filter + StatusBadge + Skeleton + EmptyState
- RecruiterPayouts: DataTable with StatusBadge + Button for withdraw + Skeleton + EmptyState
- Zero hardcoded colors across all 3 files
- TypeScript compilation passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-by-page-rollout-responsive-polish/10-01-SUMMARY.md`
</output>

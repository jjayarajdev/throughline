---
phase: 10-page-by-page-rollout-responsive-polish
plan: 05
type: execute
wave: 3
depends_on: [10-04]
files_modified: []
autonomous: false
requirements: [NFR-01, NFR-02, NFR-03, NFR-04]
must_haves:
  truths:
    - "npm run build produces zero errors and chunk sizes are documented"
    - "Lighthouse desktop performance score on RecruiterDashboard is documented (target >= 90)"
    - "All 35 pages confirmed visually correct at 3 breakpoints in both themes by human"
    - "No functional regressions from restyling -- forms submit, navigation works, data loads"
  artifacts: []
  key_links: []
---

<objective>
Final verification: build validation, bundle size check, and visual human checkpoint across all 35 pages at 3 breakpoints (375px, 768px, 1440px) in both light and dark themes.

Purpose: Confirm Phase 10 success criteria are met before marking the milestone complete.
Output: Documented build output, Lighthouse score, and human visual approval.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-page-by-page-rollout-responsive-polish/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and bundle verification</name>
  <files></files>
  <action>
  Run full build and document output:

  1. `cd apps/web && npm run build` -- capture full output including chunk sizes.
  2. Document total bundle size and largest chunks.
  3. If rollup-plugin-visualizer is available, note stats.html was generated.
  4. Run `npx tsc --noEmit --project apps/web/tsconfig.json` -- confirm zero errors.
  5. Run the final hardcoded color audit one more time:
     `grep -rn "bg-gray-\|text-gray-\|border-gray-\|bg-blue-\|bg-emerald-\|bg-purple-\|bg-amber-\|bg-red-\|bg-green-\|text-red-\|text-green-\|text-blue-\|text-emerald-\|text-purple-\|dark:bg-gray\|dark:text-gray\|dark:border-gray" apps/web/src/pages/`
     Document result (expect 0 matches).
  6. Count total page files: `find apps/web/src/pages -name "*.tsx" | wc -l` (expect 35).
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform/apps/web && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Build passes. Bundle sizes documented. Zero hardcoded colors confirmed. 35 page files counted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 35 pages at 3 breakpoints</name>
  <action>Human verifies visual quality of the full design system rollout across all pages, breakpoints, and themes.</action>
  <what-built>All 35 pages converted to design system (Phases 5-9 patterns applied across entire app). Pages converted in this phase: RecruiterWallet, RecruiterEarnings, RecruiterPayouts, CompanyWallet, CompanyFund, AdminPayouts, AdminSettings. Pages polished: auth pages (5), error pages (2), HomePage, profile pages (2), form pages (3), detail pages (2).</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (port 4001)
    2. Open Chrome DevTools -> Toggle Device Toolbar
    3. Spot-check these key pages at 375px, 768px, and 1440px in BOTH light and dark themes:

    **Recruiter flow (login as recruiter):**
    - /r/dashboard -- charts, stat cards, animations
    - /r/wallet -- balance cards, transaction table
    - /r/earnings -- summary cards, earnings table with sort/filter
    - /r/payouts -- payout history table, withdraw button
    - /r/browse -- DataTable with roles
    - /r/submissions -- DataTable, click row -> slide-over
    - /r/profile -- form with bank details section

    **Company flow (login as company):**
    - /c/dashboard -- charts, stat cards
    - /c/wallet -- balance cards, transactions
    - /c/wallet/fund -- amount presets, payment flow
    - /c/roles -- DataTable, filter tabs, click -> slide-over
    - /c/roles/new -- role creation form
    - /c/profile -- company details form

    **Admin flow (login as admin):**
    - /a/dashboard -- charts, stat cards
    - /a/payouts -- DataTable, approve/reject buttons, reject dialog
    - /a/settings -- settings cards with inputs
    - /a/users -- DataTable
    - /a/roles -- DataTable

    **Auth pages (no login needed):**
    - /login
    - /register
    - /forgot-password

    **Error pages:**
    - /nonexistent-url (404)

    **Check for:**
    - No hardcoded gray/blue/emerald colors (everything uses teal/semantic tokens)
    - Dark mode: text readable, no invisible elements, colors correct
    - Mobile (375px): sidebar collapsed, cards stacked, tables scroll horizontally
    - Tablet (768px): 2-column grids, sidebar hamburger
    - Desktop (1440px): full layout, 4-column stat cards, sidebar visible
    - No horizontal overflow at any breakpoint
    - Skeleton loading visible during data fetch (throttle network to Slow 3G to see)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 10 is complete when:
1. Build passes with zero errors
2. All 35 pages use design system tokens exclusively (grep confirms)
3. Human visual approval at 3 breakpoints in both themes
4. No functional regressions (forms work, navigation works, data loads)
</verification>

<success_criteria>
- Build passes
- Human approves visual quality at all breakpoints
- Zero hardcoded colors in codebase
- All success criteria from ROADMAP Phase 10 met
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-by-page-rollout-responsive-polish/10-05-SUMMARY.md`
</output>

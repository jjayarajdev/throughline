---
phase: 10-page-by-page-rollout-responsive-polish
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/web/src/pages/LoginPage.tsx
  - apps/web/src/pages/RegisterPage.tsx
  - apps/web/src/pages/ForgotPasswordPage.tsx
  - apps/web/src/pages/ResetPasswordPage.tsx
  - apps/web/src/pages/VerifyEmailPage.tsx
  - apps/web/src/pages/NotFoundPage.tsx
  - apps/web/src/pages/ForbiddenPage.tsx
  - apps/web/src/pages/HomePage.tsx
  - apps/web/src/pages/recruiter/RecruiterProfile.tsx
  - apps/web/src/pages/company/CompanyProfile.tsx
  - apps/web/src/pages/company/RoleCreate.tsx
  - apps/web/src/pages/company/RoleEdit.tsx
  - apps/web/src/pages/company/RoleDetail.tsx
  - apps/web/src/pages/recruiter/SubmitCandidate.tsx
  - apps/web/src/pages/recruiter/SubmissionDetail.tsx
autonomous: true
requirements: [FR-19, NFR-02]
must_haves:
  truths:
    - "Auth pages (5) use centered Card layout with Logo, responsive padding, and zero hardcoded colors"
    - "Error pages (2) use Card with icon badge, descriptive text, and CTA buttons"
    - "HomePage uses semantic tokens exclusively -- zero gray-* or emerald-* classes"
    - "Profile pages (2) use PageHeader instead of raw h1 headers"
    - "Form pages (RoleCreate, RoleEdit, SubmitCandidate) use PageHeader"
    - "Detail pages (RoleDetail, SubmissionDetail) use PageHeader"
    - "All 15 pages have responsive grid classes (grid-cols-1 sm:grid-cols-2 lg:grid-cols-*)"
  artifacts:
    - path: "apps/web/src/pages/NotFoundPage.tsx"
      provides: "404 page with Card wrapper, icon, and CTA"
      contains: "Card"
    - path: "apps/web/src/pages/recruiter/RecruiterProfile.tsx"
      provides: "Profile page with PageHeader"
      contains: "PageHeader"
  key_links:
    - from: "apps/web/src/pages/recruiter/RecruiterProfile.tsx"
      to: "@/components/custom/PageHeader"
      via: "import"
      pattern: "PageHeader"
    - from: "apps/web/src/pages/NotFoundPage.tsx"
      to: "@/components/ui/card"
      via: "import"
      pattern: "Card"
---

<objective>
Polish the 15 already-mostly-converted pages. These pages already use shadcn components and semantic tokens but need: (1) PageHeader instead of raw h1 headers, (2) any remaining hardcoded color cleanup, (3) responsive grid audit, (4) consistent empty/loading patterns. This is a light-touch pass -- no structural rewrites needed.

Purpose: Achieve 100% design system coverage across all 35 pages.
Output: 15 pages with consistent PageHeader, responsive grids, and zero hardcoded colors.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-page-by-page-rollout-responsive-polish/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish auth pages (5) + error pages (2) + HomePage</name>
  <files>apps/web/src/pages/LoginPage.tsx, apps/web/src/pages/RegisterPage.tsx, apps/web/src/pages/ForgotPasswordPage.tsx, apps/web/src/pages/ResetPasswordPage.tsx, apps/web/src/pages/VerifyEmailPage.tsx, apps/web/src/pages/NotFoundPage.tsx, apps/web/src/pages/ForbiddenPage.tsx, apps/web/src/pages/HomePage.tsx</files>
  <action>
  These 8 pages are already 90%+ converted. Apply finishing touches only:

  **Auth pages (Login, Register, ForgotPassword, ResetPassword, VerifyEmail):**
  - These already use Card + Form + Input + Button + semantic tokens. They render inside a centered auth layout.
  - Verify no stray hardcoded colors -- run a mental grep for `gray-`, `blue-`, `emerald-`, `red-` (excluding semantic tokens like `text-destructive`). These pages look clean already.
  - NO structural changes needed. Mark as verified if clean.

  **NotFoundPage:**
  - Currently minimal: just a div with h1 "404" + p + Button. Upgrade to match ForbiddenPage pattern:
    - Wrap in `Card` with `CardContent`
    - Add icon in circular badge: `<div className="mx-auto w-16 h-16 rounded-full bg-muted flex items-center justify-center"><FileQuestion className="h-8 w-8 text-muted-foreground" /></div>`
    - Keep "404" as `text-4xl font-bold`
    - Add "Page Not Found" subtitle
    - Keep existing Button + Link

  **ForbiddenPage:**
  - Already well-structured with Card, icon, CTA. No changes needed -- verify and leave as-is.

  **HomePage:**
  - Already uses Card, Badge, Button, semantic tokens (text-primary, bg-primary/10, text-muted-foreground).
  - Verify: check for any stray `gray-` or non-semantic colors in the health card section (`bg-muted` is fine, `bg-gray-*` is not).
  - The `font-mono` on health data is fine (that's a utility, not a color).
  - NO structural changes needed if no hardcoded colors found.

  **For all 8 files:** If a page is already clean, do not modify it. Only touch files that actually need changes.
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Auth pages verified clean. NotFoundPage upgraded with Card + icon badge. ForbiddenPage verified clean. HomePage verified clean. Zero hardcoded colors across all 8 files.</done>
</task>

<task type="auto">
  <name>Task 2: Polish profile, form, and detail pages (7)</name>
  <files>apps/web/src/pages/recruiter/RecruiterProfile.tsx, apps/web/src/pages/company/CompanyProfile.tsx, apps/web/src/pages/company/RoleCreate.tsx, apps/web/src/pages/company/RoleEdit.tsx, apps/web/src/pages/company/RoleDetail.tsx, apps/web/src/pages/recruiter/SubmitCandidate.tsx, apps/web/src/pages/recruiter/SubmissionDetail.tsx</files>
  <action>
  These 7 pages already use shadcn components but have raw `<header><h1>` patterns instead of PageHeader. Apply consistent polish:

  **RecruiterProfile.tsx:**
  - Replace `<header className="flex flex-col gap-1"><h1 className="text-2xl font-semibold tracking-tight">Your profile</h1><p className="text-sm text-muted-foreground">...</p></header>` with `<PageHeader title="Your Profile" description="Keep your details current so companies can find you and payouts land in the right account." />`.
  - Verify the bank details section: check for `dark:border-gray-700` in the invitation row -- replace with `border-border` if found.
  - The rest (Card, Form, Skeleton, Separator, etc.) is already clean.

  **CompanyProfile.tsx:**
  - Replace raw `<header>` with `<PageHeader title="Company Profile" description="Recruiters and candidates see this information on every gig you post." />`.
  - Rest is clean.

  **RoleCreate.tsx:**
  - Replace raw `<header>` with `<PageHeader title="Post a New Role" description="Roles start as drafts. Publish when you're ready for recruiters to see them." />`.
  - Rest is clean (uses Card, RoleForm).

  **RoleEdit.tsx:**
  - Replace raw `<header>` with `<PageHeader title="Edit Role" description={editable ? "..." : "Pause this role first to make edits."} />`.
  - Rest is clean.

  **RoleDetail.tsx:**
  - This page has a complex header with status badges and action buttons. Do NOT use PageHeader here -- the existing header with h1 + badges + action buttons is intentionally complex. Instead:
    - Check for `dark:border-gray-700` in the invitations section and replace with `border-border`.
    - Check submissions section Loader2 spinner: keep it (it's a small inline loading indicator, not a full-page loader).
    - Verify no hardcoded color classes in the ~660 line file.

  **SubmitCandidate.tsx:**
  - Replace raw `<header>` with `<PageHeader title="Submit a Candidate" description="Upload the candidate's CV and fill in their details below." />`.
  - The `@next/next/no-img-element` eslint comment is harmless (leftover from template). Leave it.
  - Rest is clean.

  **SubmissionDetail.tsx:**
  - This page has a custom header with candidate name + StatusBadge + Withdraw button. Do NOT replace with PageHeader. Instead:
    - Verify no hardcoded colors. Check for any `text-gray-*` or `bg-gray-*`.
    - The page already uses Card, Badge, Button, Skeleton, Sheet, StatusBadge, Separator -- all design system components.

  **For all files:** Only modify if actual changes needed. Do not touch files that are already clean.
  </action>
  <verify>
    <automated>cd E:/xLabs/gigcruite/platform && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Profile pages use PageHeader. RoleCreate/RoleEdit/SubmitCandidate use PageHeader. Detail pages verified clean (complex headers intentionally preserved). Zero hardcoded colors across all 7 files.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `grep -r "bg-gray-\|text-gray-\|border-gray-\|dark:bg-gray\|dark:text-gray\|dark:border-gray" apps/web/src/pages/LoginPage.tsx apps/web/src/pages/RegisterPage.tsx apps/web/src/pages/NotFoundPage.tsx apps/web/src/pages/ForbiddenPage.tsx apps/web/src/pages/HomePage.tsx apps/web/src/pages/recruiter/RecruiterProfile.tsx apps/web/src/pages/company/CompanyProfile.tsx apps/web/src/pages/company/RoleCreate.tsx apps/web/src/pages/company/RoleEdit.tsx apps/web/src/pages/recruiter/SubmitCandidate.tsx` returns 0 matches
4. Pages that gained PageHeader import it from `@/components/custom`
</verification>

<success_criteria>
- All 15 pages use design system exclusively
- Pages with simple headers now use PageHeader
- Pages with complex headers (RoleDetail, SubmissionDetail) retain custom headers but with semantic tokens only
- NotFoundPage upgraded with Card + icon badge
- Zero hardcoded colors across all files
- TypeScript compilation passes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-page-by-page-rollout-responsive-polish/10-03-SUMMARY.md`
</output>
